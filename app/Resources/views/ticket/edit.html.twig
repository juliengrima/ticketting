{% extends 'layout.html.twig' %}

{% block title %}
    Edition du ticket - n° {% for tickets in ticket %} {{ ticket.id }} {% endfor %}
{% endblock %}


{% block body %}


    <div class="container">
        {{ form_start(edit_form) }}
        <div class="row">
            <div class="col s12 m6 l4 valign-wrapper">
                {{ form_widget(edit_form.society) }}
                {{ form_errors(edit_form.society) }}
            </div>
            <div class="col s12 m6 l4 valign-wrapper">
                {{ form_widget(edit_form.user_name) }}
                {{ form_errors(edit_form.user_name) }}
            </div>
            <div class="col s12 m6 l4">

                {{ form_widget(edit_form.phone) }}
                {{ form_errors(edit_form.phone) }}

                {{ form_widget(edit_form.email) }}
                {{ form_errors(edit_form.email) }}

            </div>
        </div>
        <div class="row">
            <div class="col s12 m6 l4 center-align">
                {{ form_widget(edit_form.comment) }}
                {{ form_errors(edit_form.comment) }}
            </div>
            <div class="s12 m6 l4 right-align">
                <ul>
                    <li class="spacing">
                        <input type="submit" value="Editer" class="submit-color-edit" />
                    </li>
                    {{ form_end(edit_form) }}
                    <li>
                        {{ form_start(delete_form) }}
                        <input type="submit" value="Supprimer" class="submit-color-del">
                        {{ form_end(delete_form) }}
                    </li>
            </div>
        </div>

    </div>

{% endblock %}
